#include "core/GitGrepExecutor.hpp"

std::vector<SearchResult> GitGrepExecutor::grep(const std::string& pattern, const std::string& branch,
                                                const std::vector<std::string>& excludeBranches) {
  // 仮実装
  return {};
}
